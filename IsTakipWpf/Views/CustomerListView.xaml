<UserControl x:Class="IsTakipWpf.Views.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Müşteriler" Style="{StaticResource MaterialDesignHeadline4TextBlock}" Margin="0,0,0,20"/>

        <materialDesign:Card Grid.Row="1" Margin="0,0,0,16" Padding="16">
            <DockPanel>
                <Button DockPanel.Dock="Right" 
                        Content="YENİ MÜŞTERİ" 
                        Command="{Binding AddCustomerCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        materialDesign:ButtonAssist.CornerRadius="2"/>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBox materialDesign:HintAssist.Hint="Ara (Ad, Soyad veya Telefon)..." 
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                             Width="250"
                             Margin="0,0,16,0"
                             materialDesign:TextFieldAssist.HasClearButton="True"/>

                    <Button Command="{Binding ImportExcelCommand}" ToolTip="Excel'den İçe Aktar" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,4,0">
                        <materialDesign:PackIcon Kind="FileImport" />
                    </Button>
                    <Button Command="{Binding ExportExcelCommand}" ToolTip="Excel'e Dışa Aktar" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,4,0">
                        <materialDesign:PackIcon Kind="FileExport" />
                    </Button>
                    <Button Command="{Binding ExportPdfCommand}" ToolTip="PDF'e Dışa Aktar" Style="{StaticResource MaterialDesignFlatButton}">
                        <materialDesign:PackIcon Kind="FilePdfBox" />
                    </Button>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" materialDesign:ElevationAssist.Elevation="Dp2">
            <DataGrid ItemsSource="{Binding Customers}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="All"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ad" Binding="{Binding FirstName}" Width="Auto"/>
                    <DataGridTextColumn Header="Soyad" Binding="{Binding LastName}" Width="Auto"/>
                    <DataGridTextColumn Header="Telefon" Binding="{Binding PhoneNumber}" Width="Auto"/>
                    <DataGridTextColumn Header="Adres" Binding="{Binding Address}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="MaxHeight" Value="40"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}" 
                                            Command="{Binding DataContext.EditCustomerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Düzenle"
                                            Height="32" Width="32">
                                        <materialDesign:PackIcon Kind="Pencil" Height="16" Width="16"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}" 
                                            Command="{Binding DataContext.DeleteCustomerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Sil"
                                            Foreground="{DynamicResource ErrorBrush}"
                                            Height="32" Width="32">
                                        <materialDesign:PackIcon Kind="Delete" Height="16" Width="16"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>