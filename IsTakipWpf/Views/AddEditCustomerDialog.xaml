<UserControl x:Class="IsTakipWpf.Views.AddEditCustomerDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid Margin="16" MinWidth="350">
        <StackPanel>
            <TextBlock Text="{Binding Title}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,24"/>

            <TextBox materialDesign:HintAssist.Hint="Ad" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,16"/>

            <TextBox materialDesign:HintAssist.Hint="Soyad" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,16"/>

            <TextBox materialDesign:HintAssist.Hint="Telefon" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,16"/>

            <TextBox materialDesign:HintAssist.Hint="Adres" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" Height="80" VerticalContentAlignment="Top"
                     Margin="0,0,0,24"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource ErrorBrush}" 
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                       Margin="0,0,0,16"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="İPTAL" Style="{StaticResource MaterialDesignFlatButton}" 
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource Self}}"/>
                <Button Content="KAYDET" Style="{StaticResource MaterialDesignFlatButton}" 
                        Command="{Binding SaveCommand}"
                        Margin="8,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>