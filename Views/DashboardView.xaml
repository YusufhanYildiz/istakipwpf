<UserControl x:Class="IsTakipWpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <StackPanel>
                <TextBlock Text="Panel" Style="{StaticResource MaterialDesignHeadline4TextBlock}" Margin="0,0,0,24"/>
                
                <UniformGrid Columns="4" Margin="0,0,0,24">
                    <!-- Toplam Müşteri -->
                    <materialDesign:Card Margin="0,0,12,0" UniformCornerRadius="12" materialDesign:ElevationAssist.Elevation="Dp0">
                        <materialDesign:Card.Effect>
                            <DropShadowEffect BlurRadius="15" Direction="270" RenderingBias="Quality" ShadowDepth="2" Opacity="0.2"/>
                        </materialDesign:Card.Effect>
                        <Border CornerRadius="12">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#4A148C" Offset="0"/>
                                    <GradientStop Color="#7B1FA2" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Grid Margin="16">
                                <materialDesign:PackIcon Kind="AccountMultiple" Width="48" Height="48" Foreground="White" Opacity="0.2" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
                                <StackPanel>
                                    <TextBlock Text="Toplam Müşteri" Foreground="White" Opacity="0.9" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding TotalCustomers}" Foreground="White" FontSize="32" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </materialDesign:Card>

                    <!-- Devam Eden İşler -->
                    <materialDesign:Card Margin="0,0,12,0" UniformCornerRadius="12" materialDesign:ElevationAssist.Elevation="Dp0">
                        <materialDesign:Card.Effect>
                            <DropShadowEffect BlurRadius="15" Direction="270" RenderingBias="Quality" ShadowDepth="2" Opacity="0.2"/>
                        </materialDesign:Card.Effect>
                        <Border CornerRadius="12">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#0D47A1" Offset="0"/>
                                    <GradientStop Color="#1976D2" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Grid Margin="16">
                                <materialDesign:PackIcon Kind="Briefcase" Width="48" Height="48" Foreground="White" Opacity="0.2" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
                                <StackPanel>
                                    <TextBlock Text="Devam Eden İşler" Foreground="White" Opacity="0.9" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding ActiveJobs}" Foreground="White" FontSize="32" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </materialDesign:Card>

                    <!-- Bekleyen İşler -->
                    <materialDesign:Card Margin="0,0,12,0" UniformCornerRadius="12" materialDesign:ElevationAssist.Elevation="Dp0">
                        <materialDesign:Card.Effect>
                            <DropShadowEffect BlurRadius="15" Direction="270" RenderingBias="Quality" ShadowDepth="2" Opacity="0.2"/>
                        </materialDesign:Card.Effect>
                        <Border CornerRadius="12">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#E65100" Offset="0"/>
                                    <GradientStop Color="#FB8C00" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Grid Margin="16">
                                <materialDesign:PackIcon Kind="Clock" Width="48" Height="48" Foreground="White" Opacity="0.2" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
                                <StackPanel>
                                    <TextBlock Text="Bekleyen İşler" Foreground="White" Opacity="0.9" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding WaitingJobs}" Foreground="White" FontSize="32" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </materialDesign:Card>

                    <!-- Toplam Kalan Bakiye -->
                    <materialDesign:Card UniformCornerRadius="12" materialDesign:ElevationAssist.Elevation="Dp0">
                        <materialDesign:Card.Effect>
                            <DropShadowEffect BlurRadius="15" Direction="270" RenderingBias="Quality" ShadowDepth="2" Opacity="0.2"/>
                        </materialDesign:Card.Effect>
                        <Border CornerRadius="12">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#B71C1C" Offset="0"/>
                                    <GradientStop Color="#D32F2F" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Grid Margin="16">
                                <materialDesign:PackIcon Kind="Finance" Width="48" Height="48" Foreground="White" Opacity="0.2" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
                                <StackPanel>
                                    <TextBlock Text="Toplam Kalan Bakiye" Foreground="White" Opacity="0.9" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding TotalBalance, StringFormat=C0}" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </materialDesign:Card>
                </UniformGrid>

                <materialDesign:Card Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <materialDesign:PackIcon Kind="FileExcel" Foreground="Green" Width="28" Height="28" VerticalAlignment="Center"/>
                            <TextBlock Text="Kapsamlı Excel Veri Aktarım Rehberi" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <TextBlock Text="Dışarıdaki verilerinizi sisteme sorunsuz aktarmak için Excel dosyanızı aşağıdaki kurallara göre hazırlayın:" 
                                   Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,16" Opacity="0.8"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Müşteri Aktarımı Detaylı -->
                            <StackPanel Grid.Column="0">
                                <materialDesign:ColorZone Mode="PrimaryMid" Padding="8" CornerRadius="4" Margin="0,0,0,8">
                                    <TextBlock Text="Müşteri Aktarımı" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </materialDesign:ColorZone>
                                <TextBlock Text="Sütun Sıralaması (A'dan F'ye):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ItemsControl Margin="8,0,0,8">
                                    <TextBlock Text="1. Ad (Zorunlu) - Metin"/>
                                    <TextBlock Text="2. Soyad (Zorunlu) - Metin"/>
                                    <TextBlock Text="3. Telefon (Zorunlu) - 05XXXXXXXXX"/>
                                    <TextBlock Text="4. Adres (Opsiyonel) - Açık Adres"/>
                                    <TextBlock Text="5. İl (Opsiyonel) - Metin"/>
                                    <TextBlock Text="6. İlçe (Opsiyonel) - Metin"/>
                                </ItemsControl>
                                <materialDesign:Card Background="#FFF9C4" Padding="8">
                                    <TextBlock Text="İpucu: İlk satır başlık olmalıdır. .xlsx, .xls ve .csv formatları desteklenir. Telefon numaralarını 11 hane olacak şekilde bitişik yazınız." TextWrapping="Wrap" FontSize="11" Foreground="#F57F17"/>
                                </materialDesign:Card>
                            </StackPanel>

                            <Separator Grid.Column="1" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="16,0"/>

                            <!-- İş Aktarımı Detaylı -->
                            <StackPanel Grid.Column="2">
                                <materialDesign:ColorZone Mode="PrimaryMid" Padding="8" CornerRadius="4" Margin="0,0,0,8">
                                    <TextBlock Text="İş Takip Aktarımı" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </materialDesign:ColorZone>
                                <TextBlock Text="Sütun Sıralaması (A'dan K'ye):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ItemsControl Margin="8,0,0,8">
                                    <TextBlock Text="1. Müşteri (Zorunlu) - Ad Soyad"/>
                                    <TextBlock Text="2. İl (Opsiyonel) - Metin"/>
                                    <TextBlock Text="3. İlçe (Opsiyonel) - Metin"/>
                                    <TextBlock Text="4. İş Başlığı (Zorunlu) - Metin"/>
                                    <TextBlock Text="5. Açıklama (Opsiyonel) - Metin"/>
                                    <TextBlock Text="6. Durum (Zorunlu) - Bekliyor/Devam Ediyor/Tamamlandı"/>
                                    <TextBlock Text="7. Başlangıç (Zorunlu) - GG.AA.YYYY"/>
                                    <TextBlock Text="8. Bitiş (Opsiyonel) - GG.AA.YYYY"/>
                                    <TextBlock Text="9. İş Tutarı (Zorunlu) - Sayı (Örn: 1500)"/>
                                    <TextBlock Text="10. Alınan Ödeme (Zorunlu) - Sayı (Örn: 500)"/>
                                    <TextBlock Text="11. Kalan Bakiye (Opsiyonel) - Sayı"/>
                                </ItemsControl>
                                <materialDesign:Card Background="#E1F5FE" Padding="8">
                                    <TextBlock Text="Kritik: Durum sütunu için tam olarak 'Bekliyor', 'Devam Ediyor', 'Tamamlandı' veya 'İptal Edildi' yazılmalıdır." TextWrapping="Wrap" FontSize="11" Foreground="#0277BD"/>
                                </materialDesign:Card>
                            </StackPanel>
                        </Grid>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                            <materialDesign:PackIcon Kind="Information" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="Aktarım işlemi bittiğinde ekranın altındaki bildirim çubuğunda özet raporu görebilirsiniz." 
                                       Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Padding="24">
                    <StackPanel>
                        <TextBlock Text="Hoş Geldiniz" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                        <TextBlock Text="İş Takip Uygulamasına Hoş Geldiniz. Bu uygulama ile tüm süreçlerinizi yerel ve güvenli bir şekilde yönetebilirsiniz." 
                                   Margin="0,10,0,0" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                    </StackPanel>
                </materialDesign:Card>

                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Hızlı Not Ekle -->
                    <materialDesign:Card Grid.Column="0" Padding="16" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="Hızlı Not Ekle" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16"/>
                            <TextBox materialDesign:HintAssist.Hint="Başlık" 
                                     Text="{Binding QuickNoteTitle, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     Margin="0,0,0,8"/>
                            <TextBox materialDesign:HintAssist.Hint="Not içeriği..." 
                                     Text="{Binding QuickNoteContent, UpdateSourceTrigger=PropertyChanged}" 
                                     TextWrapping="Wrap" AcceptsReturn="True" Height="80"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     Margin="0,0,0,16"/>
                            <Button Content="Kaydet" Command="{Binding AddQuickNoteCommand}" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Son Notlar -->
                    <materialDesign:Card Grid.Column="1" Padding="16" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Son Notlar" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16"/>
                            <ItemsControl ItemsSource="{Binding LatestNotes}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource MaterialDesignDivider}" Padding="0,4">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding CreatedDate, StringFormat='dd.MM.yyyy HH:mm'}" Style="{StaticResource MaterialDesignCaptionTextBlock}" Opacity="0.6"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="Henüz not bulunmuyor." 
                                       Visibility="{Binding LatestNotes.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}" 
                                       HorizontalAlignment="Center" Margin="0,16,0,0" Opacity="0.6"/>
                        </StackPanel>
                    </materialDesign:Card>
                </Grid>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>