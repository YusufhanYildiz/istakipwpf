<UserControl x:Class="IsTakipWpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <StackPanel>
                <TextBlock Text="Panel" Style="{StaticResource MaterialDesignHeadline4TextBlock}" Margin="0,0,0,24"/>
                
                <UniformGrid Columns="3" Margin="0,0,0,24">
                    <materialDesign:Card Padding="16" Margin="0,0,16,0" Background="{DynamicResource PrimaryHueMidBrush}" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}">
                        <StackPanel>
                            <TextBlock Text="Toplam Müşteri" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                            <TextBlock Text="{Binding TotalCustomers}" FontSize="36" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Padding="16" Margin="0,0,16,0" Background="LightBlue">
                        <StackPanel>
                            <TextBlock Text="Devam Eden İşler" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                            <TextBlock Text="{Binding ActiveJobs}" FontSize="36" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Padding="16" Background="Orange">
                        <StackPanel>
                            <TextBlock Text="Bekleyen İşler" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                            <TextBlock Text="{Binding WaitingJobs}" FontSize="36" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </materialDesign:Card>
                </UniformGrid>

                <materialDesign:Card Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <materialDesign:PackIcon Kind="FileExcel" Foreground="Green" Width="28" Height="28" VerticalAlignment="Center"/>
                            <TextBlock Text="Kapsamlı Excel Veri Aktarım Rehberi" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <TextBlock Text="Dışarıdaki verilerinizi sisteme sorunsuz aktarmak için Excel dosyanızı aşağıdaki kurallara göre hazırlayın:" 
                                   Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,16" Opacity="0.8"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Müşteri Aktarımı Detaylı -->
                            <StackPanel Grid.Column="0">
                                <materialDesign:ColorZone Mode="PrimaryMid" Padding="8" CornerRadius="4" Margin="0,0,0,8">
                                    <TextBlock Text="Müşteri Aktarımı" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </materialDesign:ColorZone>
                                <TextBlock Text="Sütun Sıralaması (A'dan F'ye):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ItemsControl Margin="8,0,0,8">
                                    <TextBlock Text="1. Ad (Zorunlu)"/>
                                    <TextBlock Text="2. Soyad (Zorunlu)"/>
                                    <TextBlock Text="3. Telefon (Örn: 05xx...)"/>
                                    <TextBlock Text="4. Açık Adres"/>
                                    <TextBlock Text="5. İl"/>
                                    <TextBlock Text="6. İlçe"/>
                                </ItemsControl>
                                <materialDesign:Card Background="#FFF9C4" Padding="8">
                                    <TextBlock Text="İpucu: İlk satır başlık olmalıdır. Telefon numaralarını bitişik yazmanız önerilir." TextWrapping="Wrap" FontSize="11" Foreground="#F57F17"/>
                                </materialDesign:Card>
                            </StackPanel>

                            <Separator Grid.Column="1" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="16,0"/>

                            <!-- İş Aktarımı Detaylı -->
                            <StackPanel Grid.Column="2">
                                <materialDesign:ColorZone Mode="PrimaryMid" Padding="8" CornerRadius="4" Margin="0,0,0,8">
                                    <TextBlock Text="İş Takip Aktarımı" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </materialDesign:ColorZone>
                                <TextBlock Text="Sütun Sıralaması (A'dan H'ye):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ItemsControl Margin="8,0,0,8">
                                    <TextBlock Text="1. Müşteri Ad Soyad (Sistemle aynı olmalı)"/>
                                    <TextBlock Text="2. [Boş Bırakılabilir - İl]"/>
                                    <TextBlock Text="3. [Boş Bırakılabilir - İlçe]"/>
                                    <TextBlock Text="4. İş Başlığı (Zorunlu)"/>
                                    <TextBlock Text="5. İş Açıklaması"/>
                                    <TextBlock Text="6. [Boş Bırakılabilir - Durum]"/>
                                    <TextBlock Text="7. Başlangıç Tarihi (GG.AA.YYYY)"/>
                                    <TextBlock Text="8. Bitiş Tarihi (GG.AA.YYYY)"/>
                                </ItemsControl>
                                <materialDesign:Card Background="#E1F5FE" Padding="8">
                                    <TextBlock Text="Kritik: İşin yüklenebilmesi için 'Müşteri Ad Soyad' bilgisi sistemdeki müşteriyle birebir aynı olmalıdır." TextWrapping="Wrap" FontSize="11" Foreground="#0277BD"/>
                                </materialDesign:Card>
                            </StackPanel>
                        </Grid>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                            <materialDesign:PackIcon Kind="Information" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="Aktarım işlemi bittiğinde ekranın altındaki bildirim çubuğunda özet raporu görebilirsiniz." 
                                       Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Padding="24">
                    <StackPanel>
                        <TextBlock Text="Hoş Geldiniz" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                        <TextBlock Text="İş Takip Uygulamasına Hoş Geldiniz. Bu uygulama ile tüm süreçlerinizi yerel ve güvenli bir şekilde yönetebilirsiniz." 
                                   Margin="0,10,0,0" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>